import Relay from "react-relay";

const {
  RelayEnvironmentProvider,
  useFragment,
  useMutation,
  useLazyLoadQuery,
  useSubscription,
  graphql,
} = Relay;

export {
  RelayEnvironmentProvider,
  useFragment,
  useMutation,
  useLazyLoadQuery,
  useSubscription,
  graphql,
};
